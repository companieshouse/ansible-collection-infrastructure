# Role: Linux Reboot | ansible-collection-infrastructure

Reboots a Linux instance.

Use this if your playbook makes a change that requires a reboot, it will wait until the server is back online and then continue with the next step in your playbook.
