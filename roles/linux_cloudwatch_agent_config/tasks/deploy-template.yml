- name: Template Cloudwatch agent config file
  ansible.builtin.template:
    src: templates/cloudwatch-config.json
    dest: "{{ working_dir }}/{{ cloudwatch_config_file }}"
    mode: 0644
    force: true
  notify: Reload CloudWatch Agent
